@using EMarket.Core.Application.ViewModels.Categories;
@model SaveCategoryViewModel
@{
    bool editMode = Model.Id == 0 ? true : false;
    ViewData["Title"] = "Guardar categoria";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-dark text-light">
                <h4>@(editMode ? "Create a new Category" : $"Editing the Category: {Model.Name}")</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Category" asp-action="@(editMode ? "Create" : "Edit")">

                    <input type="hidden" asp-for="Id" />

                    <div class="form-group mb-3">
                        <label asp-for="Name" class="form-label">Name:</label>
                        <input type="text" asp-for="Name" placeholder="Enter a name..." class="form-control shadow-sm">
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Description" class="form-label">Description:</label>
                        <textarea asp-for="Description" class="form-control" placeholder="My category description..."></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="container-fluid text-center">
                        <button type="submit" class="btn btn-success shadow-sm">Save</button>
                        <a asp-controller="Category" asp-action="Index" class="btn btn-secondary shadow-sm">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

